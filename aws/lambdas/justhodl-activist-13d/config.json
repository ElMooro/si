{
  "runtime": "python3.12",
  "memory": 512,
  "timeout": 300,
  "handler": "lambda_function.lambda_handler",
  "role_arn": "arn:aws:iam::857687956942:role/lambda-execution-role",
  "description": "Activist 13D scanner (Edge #9). Crawls SEC EDGAR per-filer JSON for 17 curated activists (Elliott/Trian/Pershing/Icahn/etc), parses targets, age-decayed forward-return priors, per-setup trade ticket.",
  "inherit_env": true,
  "env": {
    "S3_BUCKET": "justhodl-dashboard-live"
  }
}
