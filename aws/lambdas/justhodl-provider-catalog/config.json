{
  "function_name": "justhodl-provider-catalog",
  "region": "us-east-1",
  "runtime": "python3.12",
  "handler": "lambda_function.lambda_handler",
  "memory": 1024,
  "timeout": 900,
  "ephemeral_storage": 2048,
  "role": "arn:aws:iam::857687956942:role/lambda-execution-role",
  "description": "Universal provider catalog: scans all 57 data sources, publishes paged inventories, compact provider shards, and the SQLite FTS index used by Chart Pro search; hourly, matching the observed EventBridge rule.",
  "eventbridge_rules": [
    "justhodl-provider-catalog-hourly"
  ],
  "env": {
    "S3_BUCKET": "justhodl-dashboard-live"
  },
  "schedule": {
    "rule_name": "justhodl-provider-catalog-hourly",
    "cron": "rate(1 hour)",
    "description": "Hourly provider catalog refresh; existing cadence verified by ops 5283."
  }
}
