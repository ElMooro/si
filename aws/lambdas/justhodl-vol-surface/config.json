{
  "timeout": 300,
  "schedule": "cron(30 13 * * ? *)",
  "description": "Source-defined Cboe volatility research with protected originals, deterministic replay, matched observation dates and explicit abstention",
  "memory": 256
}
