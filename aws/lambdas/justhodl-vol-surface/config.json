{
  "memory_size": 256,
  "timeout": 120,
  "schedule": "cron(20 13-21 ? * MON-FRI *)",
  "description": "Vol Surface v3 — FRED VIX-family + CBOE SKEW. Cross-asset vol (8 markets) + term structure + tail premium + composite stress score. Replaces broken Polygon/Yahoo paths."
}
