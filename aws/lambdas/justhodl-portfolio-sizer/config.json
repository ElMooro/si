{
  "function_name": "justhodl-portfolio-sizer",
  "region": "us-east-1",
  "runtime": "python3.12",
  "handler": "lambda_function.lambda_handler",
  "memory": 512,
  "timeout": 180,
  "architectures": ["x86_64"],
  "role": "arn:aws:iam::857687956942:role/lambda-execution-role",
  "description": "REAL portfolio Kelly recommender (DDB-integrated). Reads positions + risk + alpha + confluence + Macro Stress Score, outputs sized recommendations per position + entry candidates. Replaces paper-portfolio sizer-v2 with DDB integration.",
  "environment": {},
  "inherit_env": [
    {"from_function": "justhodl-options-flow-scanner", "keys": ["POLY_KEY"]},
    {"from_function": "justhodl-screener-alerts", "keys": ["TELEGRAM_TOKEN", "TELEGRAM_CHAT_ID"]}
  ],
  "schedule": {
    "rule_name": "justhodl-portfolio-sizer-hourly",
    "cron": "cron(45 * * * ? *)",
    "description": "Every hour at :45 - 2 min after portfolio-risk (:43) so risk metrics fresh"
  }
}
