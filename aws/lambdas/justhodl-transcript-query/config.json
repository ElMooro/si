{
  "runtime": "python3.12",
  "memory": 1024,
  "timeout": 30,
  "handler": "lambda_function.lambda_handler",
  "role_arn": "arn:aws:iam::857687956942:role/lambda-execution-role",
  "description": "Transcript Query Handler (R2b). On-demand TF-IDF ranked search over data/transcripts-index.json. POST {query, ticker?, quarter_prefix?, limit?}. Returns ranked chunks w/ score, matched_terms. Function URL enabled.",
  "env": {
    "S3_BUCKET": "justhodl-dashboard-live"
  },
  "function_url": {
    "AuthType": "NONE",
    "Cors": {
      "AllowOrigins": ["*"],
      "AllowMethods": ["POST", "OPTIONS"],
      "AllowHeaders": ["Content-Type"],
      "MaxAge": 86400
    }
  }
}
