{
  "timeout": 180,
  "description": "Public retail attention samples: retained originals, explicit missing baselines and descriptive-only replay; existing daily rule unchanged",
  "memory": 512,
  "schedule": "cron(10 19 * * ? *)"
}
