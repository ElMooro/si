{
  "function_name": "justhodl-alpha-compass",
  "region": "us-east-1",
  "runtime": "python3.12",
  "handler": "lambda_function.lambda_handler",
  "memory": 512,
  "timeout": 240,
  "architectures": [
    "x86_64"
  ],
  "role": "arn:aws:iam::857687956942:role/lambda-execution-role",
  "description": "Reproducible public Alpha research: dated input projections, separate descriptive cohorts, explicit qualification gaps. No private account inputs, automatic sizing, paid AI or alerts.",
  "environment": {},
  "schedule": {
    "rule_name": "alpha-compass-3h",
    "cron": "cron(50 */3 * * ? *)",
    "description": "Alpha Compass synthesis \u2014 every 3 hours at :50, just after conviction-engine (:45) updates"
  }
}
