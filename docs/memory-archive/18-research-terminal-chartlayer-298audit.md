# Memory archive — edit #18 (migrated verbatim 2026-07-08, Khalid-approved memory diet)

Source: Claude memory edit #18. This file is the authoritative archive; the memory slot now holds a one-line pointer here. Grep this directory before building anything fleet-related.

---

RESEARCH-TERMINAL + PAGE-DEPTH + CHART LAYER. Shared toolkit aws/shared/equity_enrich.py (bundled into every Lambda): fetch_financials (10yr revenue/margins/FCF/EPS/shares, P/E vs industry+range pctile, PEG, EV/EBITDA, momentum spark, beats, segments, insider), fetch_peer_pe, load_confirmation_feeds, make_thesis(tier=bulk Haiku, THESIS:/BEAR:), grade_track_record(forward vs SPY). PER-STOCK terminals: justhodl-bottleneck-research (bottleneck-boom.html), justhodl-alpha-research (alpha-scoreboard.html 6h), justhodl-opportunities-research (opportunities.html daily 14:30). GOTCHA: new research Lambdas DON'T inherit ANTHROPIC_API_KEY→ops-patch from justhodl-research-critique via update_function_configuration. deploy-lambdas.yml supports .schedule{rule_name,cron,description}→EventBridge. HONESTY MANDATE: thesis ends on real risk, bear has explicit falsifier, scorecard shows disagreement, track records real forward vs SPY. — SHARED CHART COMPONENT /jh-enhance.js (2026-06-17, repo root→Pages→justhodl.ai/jh-enhance.js): ONE declarative <script> per page — data-feed="data/X.json" + one of data-bars="arrayPath:labelField:valueField" (ranked gradient bars, neg=red), data-line="series.path" (interactive SVG line; data-crisis="1" adds 8 crisis bands + crosshair date/value tooltip), or data-metrics="dotpath:Label|dotpath:Label" (named scalar bars, same-scale only). getJSON falls back justhodl.ai→PX→S3; inserts dark card after first <h1>. REUSE IT for any new page chart — don't rebuild. ~65 data pages wired across Tier-C batches. — 298-PAGE AUDIT (2026-06-17): redundancy MINIMAL (signal-* 5 + global-* 4 only; the feared 294-page sprawl is NOT real → consolidation low priority; real gap was VISUALIZATION). Tier A fixes: ask.html (produced data/askdesk-config.json={url,k} from ask-desk Function URL), why-cross-signal.html (dead vol-squeeze.json→squeeze-pretrigger.json), downloads.html (insecure http s3-website→https justhodl.ai). Tier B empty (charts.html is a legit meta-refresh redirect). Tier D: directory.html already lists ~292 pages (every "orphan" reachable there, 1 click from home) — DON'T bloat top nav. LEAVE-AS-TABLE (no forced chart, institutional judgment): brain(9198 notes), journal(empty trade journal), dealer-survey, catalyst-calendar, heatmap/valuations/market-map(already viz tables), funding-plumbing(mixed-scale signals). plumbing.html + sovereign.html got bespoke deep-history charts (data/plumbing-history.json = 28 indicators monthly 1990→ + 8 crisis windows, built by plumbing-aggregator; each indicator row = sparkline + click→big interactive crosshair chart; settlement-fails retains full 2013→). FAILS DATA FLOOR: NY Fed dealer-fails consistent FR2004 series starts 2013 (1998-2013=incompatible methodology break per PD seriesbreaks) — does NOT reach 1996; never claim otherwise.
