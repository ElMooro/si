# Memory archive — edit #13 (migrated verbatim 2026-07-08, Khalid-approved memory diet)

Source: Claude memory edit #13. This file is the authoritative archive; the memory slot now holds a one-line pointer here. Grep this directory before building anything fleet-related.

---

SHIP LOOP. PUSH: cd ~/work/si; edit; git add X; git -c user.email=raafouis@gmail.com -c user.name=Khalid commit -m msg; git push. 4 DEPLOY PATHS: (1) root *.html → "Deploy site to Pages" workflow (pages.yml, GitHub Pages, build_type=workflow, CNAME justhodl.ai; CF fronts as CDN) → justhodl.ai. ⭐ Committing *.html to main IS the deploy — NEVER aws s3 upload an HTML page (S3 justhodl-dashboard-live = DATA origin for data/*.json, NOT HTML origin). pages.yml build job assembles _site (cp *.html/*.js/*.css/nav-manifest) → deploy job actions/deploy-pages@v4. page-gate.yml only LINT-checks changed pages (forbidden [object Object]/undefined%); does NOT deploy. ~1-3min propagate. ⚠️⚠️ INTERMITTENT PAGES-DEPLOY FAILURE = GitHub-side INFRA FLAKE (NOT user-fixable): deploy-pages@v4 fetches the Pages artifact from a ROTATING Azure blob host productionresultssaNN.blob.core.windows.net; some hosts intermittently unreachable from the runner → "Host not in allowlist … Add this host to your network egress settings" → deploy job fails. ⚠️ There is NO egress-allowlist setting to change: ElMooro is a personal/FREE User account (orgs/ElMooro/settings/network-configurations = 404), and hosted-runner egress config is Enterprise-only — my earlier "add *.blob to allowlist" advice was WRONG. The artifact host is fixed per run, so rerun-failed-jobs reuses it (re-fails); only a FRESH run redraws the host. ⭐⭐ DURABLE FIX = SELF-HEALING pages.yml (built+validated e2e this session): deploy step is continue-on-error (non-fatal → no false-alarm failure notifications) exposing job output ok=steps.deployment.outcome; a 3rd job `self-heal` (needs:[build,deploy]; if: always() && build==success && deploy.outputs.ok!='success') RE-DISPATCHES a fresh run via `gh workflow run pages.yml --ref main -f retry_count=N+1`. ⚠️ MUST use secret PAGES_PAT (=deploy PAT, added via sealed-box API) NOT GITHUB_TOKEN (recursion-prevention silently drops GITHUB_TOKEN workflow_dispatch → no cascade) AND env GH_REPO:${{github.repository}} (self-heal job has NO checkout, so gh can't infer the repo → step fails silently without it). Deterministic circuit breaker = workflow_dispatch input retry_count (default '0', cap ≥4 → stop+::warning::). PROVEN: forced-fail run → self-heal fired → fresh run spawned ~25s later → succeeded → chain terminated. Deploys now auto-heal with zero manual action; manual FRESH workflow_dispatch pages.yml remains the ultimate fallback if the breaker trips. (2) aws/lambdas/*/source/**+config.json → deploy-lambdas.yml; (3) cloudflare/workers/*/** → deploy-workers.yml; (4) aws/ops/pending/*.py → run-ops.yml (PUSH runs only files in git diff HEAD^ HEAD; DISPATCH runs the one input.script; ⚠️ pushing+dispatching the SAME ops = DUP-RACE, both collide on report commit-back, loser reports 'failure' though work succeeded → commit ops-only change and let PUSH trigger it, DON'T also dispatch). POST: sleep 90-180; git pull. ⚠️ Sandbox egress = api.github.com + pypi + npm ONLY → CANNOT curl justhodl.ai/S3 NOR download Actions job LOGS (they 302 to *.blob.core.windows.net = blocked; read job STATUS/outcome via runs/{id}/jobs API instead, not raw logs) → verify liveness from ops RUNNER (urllib+real UA). pip needs --break-system-packages. Adding a repo secret: pip install pynacl, GET actions/secrets/public-key, sealed-box encrypt, PUT actions/secrets/NAME.
