# Memory archive — edit #30 (migrated verbatim 2026-07-08, Khalid-approved memory diet)

Source: Claude memory edit #30. This file is the authoritative archive; the memory slot now holds a one-line pointer here. Grep this directory before building anything fleet-related.

---

TV NOTES PARKED (2956-62): bookmarklet UNTESTED. — ⭐⭐ COMPASS v1.1+v1.2 + FLEET AUDIT + U1-U7 (2966-83): 31-asset compass 22:15 schema 1.2 (LEDGER vintage#1 grades 2027-07-02; ⚠️ols_beta returns (beta,n)); 3 HUBS + 9 stubs; 8/8 umbrellas SHIPPED. — ⭐⭐⭐ GAP-BUILD (2977-81): gap-metrics 21:45 11/11 (stock-bond +0.415; bills 80.8%; revision-breadth WARMING 07-28). — ⭐⭐⭐ PARALLEL RECONCILE (2984-91): collision 2984-86 → ops jumped 2990+, avoid concurrency; U9 DONE {WIRED 585, NO_OUTS 83}; ALLOCATOR←COMPASS LIVE (data/master-allocation.json, duration gate ON); SIFMA DEGRADED self-heals; OPEN=M21. — ⭐⭐⭐⭐ INDUSTRY ROTATION v1 (2992-97): daily 21:35 data/industry-rotation.json. ⚠️⚠️FMP `/stable/etf-holder`=LEGACY backwards → **`/stable/etf/holdings`**; resilience.json NO sector field → soldiers = holdings ∩ resilience TICKERS. ⚠️⚠️DEPLOY DOCTRINE: (a) new fns NO env → copy donor; (b) ensure_eb_rule() ONLY; (c) CRASH-GUARD every ops (3002: exact KeyError+line in report despite blocked logs); (d) short+long env names; (e) create-branch INTERMITTENT NO-OP on green → io.BytesIO zipfile→create_function from runner checkout (2999 proven). — ⭐⭐⭐⭐⭐ THEORY-STACK (2998-99, 15-theory email; factor-returns=X-sectional L/S DISTINCT do-NOT-merge): **IR v2.0**: JT blend (0.5×3m+0.3×6m-skip+0.2×12-1) BETA-ADJUSTED (BAB); dump/rebound_excess_bps; CROWDED (2y peak≥95th+cur≥85th+slope<0); breadth BROAD≥60/NARROW<40; PEAD+. **justhodl-factor-regime** 21:28 data/factor-regime.json: 11 style ratios → states + fresh-THRUST (≥40/60 below rolling-50dMA, crossed within 10 ⚠️rockets cross decayed MA in 2-3d) + risk_appetite z. — ⭐⭐⭐⭐⭐ FUSION+CREDIT+SCORECARD (3000-3003): **BS FUSION**: _industry_prior [0.94-1.06] (⚠️_FINVIZ_TO_GICS map REQUIRED: Healthcare→Health Care, Consumer Cyclical→Consumer Discretionary, Financial (Services)→Financials, Consumer Defensive→Consumer Staples, Basic Materials→Materials) + _factor_appetite [0.97-1.03] HIGH_BETA only; >60h stale→neutral; anchors before `# ── Fed nowcast`/after _nc_mult/after risk_regime_mult/before by_verdict; offline harness = regex-extract → `def _shell(read_json,RR_HIGH_BETA):`. LIVE: GILD ×1.053, META+DIS ×0.88. **IR v2.1 CREDIT (CDS doctrine)**: Altman-Z per holding (/stable/financial-scores list[0].altmanZScore) top-8 per ETF; **DANGER ≥30% Z<1.8 or med<1.8**; banks EXCLUDED fin_skip={XLF,KRE,KBE}; **BREAKDOWN+DANGER→CONFIRMED_DETERIORATION**; resilience_read (RESILIENT_LEADER/DEFENSIVE_ONLY/HIGH_BETA/WEAK); ratio_3m_high; soldier chain vs_etf/vs_spy/chain_intact; universe **40** (+XES COPX XAR GRID WCLD XTN XHB). DAY-1: XTN 95-ladder but DANGER 75% distressed medZ 1.44 dump −43bp (the trap); **KWEB first CONFIRMED_DETERIORATION**. **BS credit-penalty + IR v3.0 SCORECARD (3003)**: CONFIRMED_DETERIORATION ×0.90 (⚠️old `==BREAKDOWN` check would SILENTLY MISS upgraded tags — fixed), sector DANGER ×0.95 (armed, 0 fires — no SPDR sector DANGER); **VOLUME LAYER zero extra API cost** (aggs carry v): updown_vol_ratio_20d SUM-basis, vol_5d_vs_50d, pullback_dryup; **scorecard_100 = email EXACT weights** abs25/relRS25(incl 3m-high 5)/MA15/resil15/breadth10/vol5/fund5, scorecard_basis honest (core85→full100 leaders), bands 80/65/50/35; leadership_score KEPT for tags+history; ladder sorts by scorecard; fundamental_confirm = holdings ∩ (PEAD+ ∪ estimate-revisions); config 420s/1024MB; ⚠️row key ratio_above_50d. DAY-1: 40/40 scored+volume, bands {LEAD 1, SW 8, N 13, W 12, AVOID 6} — **only XBI 84.3 clears 80** (CIBR 99-ladder→75.6 once dump-day bites = real discrimination); XTN 72.8 SW + DANGER same row. — ⭐⭐⭐⭐ FLOWS + ORPHAN TRIAGE (3004-3005 PASS, 2026-07-08): **flows join FINAL: etf-flows/daily.json "metrics" map** (⚠️NO per-ticker files, NO rows in data/etf-fund-flows.json — two wrong guesses before reading writer's put_object; ground-truth the writer FIRST). LIVE **35/40 joined**: KRE +$1.06B/21d, XBI +$962M, XLV +$585M (money INTO leaders = flow confirmation), IYT −$176M; flow_label null in daily metrics (label lives elsewhere, dollars display fine); 5 misses = outside 300-ETF flows universe. **ORPHAN-STALE/DEAD TRIAGE CLOSED with evidence**: recomputed from registry outs × S3 head ages (fresh<48h/stale-30d/dead>30d), poked silents with Event invokes, scheduled ONLY proven-writers: **6 RESURRECTED+SCHEDULED daily ~14:0X** (engine-contribution, causality-scanner, behavior-mirror, stress-loadings, opportunity-calibrator, beneish) + 4 REFRESHED-had-rules (nyfed-pd, forward-orders, edge-discovery, forward-returns area) + **7 reclassified ON-DEMAND no-fix** (history-api, trade-journal, transcript-indexer, transcript-query, watchlist, feedback, kill-switch — user-action writers; add to future triage skip-regex) + **engine-robustness = WARMING not broken** (sync invoke 1.1s clean: "no_engines_with_sufficient_history, min_snapshots 10" — honest waiter like interpretation-grader) + **ici-flows SOURCE_DEAD root-caused** (sync capture: RuntimeError line 281 "no ICI data (mmf=0 ltf=0) — seed histories"; matches known ICI anti-scraped .xls move; QUEUED FIX = re-source MMF/fund-flow series from FRED equivalents, real build not patch) + 1 REGISTRY_GHOST. QUEUED: ici-flows FRED re-source; M21/TV-notes/Telegram/repo-private on Khalid; ledger 2027-07; breadth self-upgrades 07-28. Last ops=3005.

## ops 3311-3323 — analyst-actions + analyst-consensus re-sourced off dead Benzinga onto FMP /stable (2026-07-15)
**Symptom:** analyst-actions.html stuck "loading" — engine wrote empty JSON because Benzinga harvest returned 0.
**Root cause (proven, not billing):** Massive stopped serving Benzinga to this account. ALL 3 Massive keys (beautiful_chandrasekhar …ptM, desperate_lamarr …VFPI, Default …JX_d) return 403 NOT_AUTHORIZED on api.polygon.io/benzinga/v1/* (REST) AND Benzinga absent from flatfiles S3 bucket (files.massive.com/flatfiles has only market data: crypto/forex/futures/indices/options/us_stocks_sip). Legacy direct Benzinga key bzMJ62… = 401 expired. Base Polygon data works (200) on …ptM, so keys valid but lack Benzinga scope.
**Fix (Path 2):** re-sourced both engines onto FMP /stable (already-paid FMP_KEY …S8xb).
- NEW aws/shared/fmp_analyst.py: fetch_ratings() from grades-latest-news (fields symbol/newGrade/previousGrade/gradingCompany/action/publishedDate) + price-target-latest-news (symbol/priceTarget/adjPriceTarget/priceWhenPosted/analystCompany), returns engine's exact record shape (rating_dir/pt_dir/pt_pct/importance=3). fetch_guidance()/fetch_analyst_insights()=[] (FMP /stable has no guidance feed). justhodl-analyst-actions v2.0.0 — VERIFIED 2310 signals (upgrades 85, downgrades 76, pt_raises 831), top pick FCEL.
- justhodl-analyst-consensus: 3 dead FMP endpoints fixed — grades-news→grades-latest-news; earnings-surprises-bulk/earnings-surprises→earnings (fields epsActual/epsEstimated, was actualEarningResult/estimatedEarning, skip null future quarters). VERIFIED: strongest_upgrades_30d (CSX), weakest_downgrades_30d (BBY), beat_kings (NVDA 100% 8/8) all populated; 194 universe.
**⚠️ GOTCHA — FMP /stable feed renames (2026):** bulk feeds now need /*-latest-news (grades-latest-news, price-target-latest-news); old grades-news/price-target-news/earnings-surprises 400 on "missing symbol". earnings-surprises→earnings. Any other engine still on old names is silently returning empty — audit before trusting.
**Open:** Benzinga guidance breadth + importance-weighting retired; re-add as enrichment if Massive support enables the Benzinga entitlement (give them request_id 861259a29c0b7f590e1263dcdf0f291d). Next ops 3324.

## ops 3324-3329 — FLEET FMP /stable rename sweep (2026-07-15)
Swept whole Lambda fleet for engines silently returning EMPTY on renamed FMP /stable feeds. Method: enumerate all distinct /stable endpoints (ops 3324) → re-probe ambiguous ones with correct fleet params (3325) → confirm exact working forms (3326-27) → patch only proven-broken LIVE calls → verify feeds fresh+populated (3329).
**6 engines fixed (all verified fresh + non-empty via 3329):** rating-change-cluster (grades-news→grades-latest-news, buy_picks flowing), sellside-views (same), 52wk-quality-breakout (earnings-surprises→earnings w/ reported-row filter, picks=8), starmine (earnings-surprises→earnings, top/bottom_25_conviction=25 each; SLOW ~2min full-universe run), buyback-scanner (insider-trading→insider-trading/search, top_opportunities=14), insider-sell-cluster (market-wide insider-trading→insider-trading/latest?transactionType=S-Sale, clusters flowing).
**Endpoint rename map (FMP /stable 2026):** grades-news→grades-latest-news; price-target-news→price-target-latest-news; earnings-surprises/earnings-surprises-bulk→earnings (epsActual/epsEstimated, skip null future qtrs); insider-trading→insider-trading/search (per-symbol) OR insider-trading/latest (market-wide); news→news/general-latest; peers→stock-peers; historical-price-eod→…/light or …/full; analyst-estimates NEEDS period param. STILL DEAD (no drop-in): mutual-fund-info (cef-discount enrichment, handles None gracefully).
**GOTCHA (cost me a cycle):** git reset --hard during ops-polling loops WIPES uncommitted str_replace edits in the sandbox working tree — commit code edits BEFORE any poll loop that resets. Also: heavy per-symbol engines (starmine/52wk) can hang a synchronous LAM.invoke in a verify-ops past timeout — verify via read-only S3 freshness (ops 3329 pattern) NOT invoke-and-wait. Next ops 3330.

## ops 3330-3332 — benzinga.html re-sourced onto FMP (2026-07-15)
Page benzinga.html (Benzinga terminal: analyst ratings, earnings cal, economic cal, breaking news) stuck "Loading..." — its agent benzinga-news-agent hit api.benzinga.com/api/v2 with the dead direct key (bzMJ62…, 401). Page fetches Function URL https://qgmut34alss5bvacffyklqqs3a0ckday.lambda-url.us-east-1.on.aws/ expecting {ts, analyst_ratings[], earnings_calendar[], economic_events[], market_news[]}.
**Fix:** benzinga-news-agent v2.0 REWRITTEN onto FMP /stable (FMP_KEY set on env, dead BENZINGA_API_KEY removed). Sources: grades-latest-news + price-target-latest-news → analyst_ratings; earnings-calendar → earnings_calendar; economic-calendar (country=US filter) → economic_events; news/general-latest → market_news; dividends-calendar → dividends (BONUS new section). Emits BOTH legacy + FMP field names (page JS reads with fallbacks: ticker||symbol, eps_est||epsEstimated, etc.) so page JS UNTOUCHED. VERIFIED end-to-end via Function URL: HTTP 200, counts ratings 40/earnings 30/economics 20/news 15/dividends 20.
**FMP calendar/news field shapes (3330):** earnings-calendar{symbol,date,epsEstimated,revenueEstimated}; economic-calendar{date,country,event,previous,estimate,actual,impact,unit}; news/general-latest & news/stock-latest{symbol,publishedDate,publisher,title,text,site,url}; dividends-calendar{symbol,date,dividend,yield,frequency}. Session total: 9 engines moved off dead Benzinga/renamed-FMP. Next ops 3333.

## ops 3333-3339 — benzinga.html 'Failed to fetch' = STALE CLOUDFLARE EDGE CACHE (2026-07-15)
After benzinga-news-agent v2.0 fix (data verified 200), page showed 'Failed to fetch'. Diagnosis chain: Function URL config CLEAN (AuthType NONE, resource policy public InvokeFunctionUrl, CORS AllowOrigins:['*'], live GET 200 w/ access-control-allow-origin:justhodl.ai). Agent latency fine (0.57s cold). ROOT CAUSE (ops 3337): ⚠️⚠️ CLOUDFLARE serving STALE jh-nav-drawer.js — bare URL = GEN 3310, cf-cache HIT, age 28684s, max-age=14400 (4h TTL); cache-busted URL = GEN 3335 fresh. So CF handed every visitor the OLD FRESH_GUARD which self-marked done → never self-healed. Affects ALL site JS for up to 4h after any deploy.
**⚠️ CF PURGE STILL DENIED:** CLOUDFLARE_API_TOKEN verifies (200) + reads zone (fb59e2d0) but purge_cache (files AND purge_everything) = 401 code 10000 'Authentication error' — token LACKS Zone>Cache Purge (grant pending, Khalid). Cloudflare MCP connector has NO cache-purge tool (only Workers/D1/KV/R2/Hyperdrive).
**FIX (no purge needed):** version the script URL. Bumped JH_FRESH_GUARD GEN 3310→3335 in jh-nav-drawer.js AND appended ?v=3335 to jh-nav-drawer.js across ALL 371 pages (sed). New URL = guaranteed CF MISS → every page serves fresh guard → clients unregister stale SW + clear Cache Storage + reload once → self-heal. VERIFIED: jh-nav-drawer.js?v=3335 → GEN 3335, cf-cache MISS; benzinga.html references ?v=3335 + funcurl present.
**DOCTRINE:** after editing any shared JS (jh-nav-drawer.js/jh-page-ai.js/interp-kit.js/sw.js), CF's 4h edge TTL means bare-URL clients stay stale — BUMP THE ?v= QUERY on the script tag sitewide (or bump FRESH_GUARD GEN) so the new file actually reaches clients. Until CF token gets Zone>Cache Purge, versioned-URL bump is the reliable delivery mechanism. Next ops 3340.

## ops 3340 — TWO CORS bugs surfaced after cache cleared (2026-07-15)
Once FRESH_GUARD delivered the real page, browser console showed the true blockers:
1. ⚠️ **benzinga Function URL DOUBLED ACAO**: `Access-Control-Allow-Origin: '*, https://justhodl.ai'` (browser rejects multi-value). CAUSE: Lambda Function URL native CORS (AllowOrigins ['*']) AND the handler code BOTH emit Access-Control-Allow-* → they stack. FIX: handler now returns ONLY {Content-Type}; Function URL native CORS is SOLE owner. ⭐ DOCTRINE: when a Lambda has Function-URL native CORS, the handler must NOT also set Access-Control-* headers or they double. (urllib doesn't merge them so ops 3333 missed it — only a real browser shows the doubled header.)
2. ⚠️ **data-proxy /userdata/self preflight rejected Authorization**: worker corsHeaders() Allow-Headers lacked 'Authorization' but /userdata/self REQUIRES Authorization: Bearer <jwt> (line ~620). FIX: added Authorization to Access-Control-Allow-Headers in cloudflare/workers/justhodl-data-proxy/src/index.js. (This runs on EVERY page via drawer account-sync, so the error was noisy site-wide, not benzinga-specific.)
VERIFIED: benzinga ACAO single-value (justhodl.ai) 200; /userdata/self OPTIONS 204 w/ Authorization allowed. benzinga.html now loads fully. Next ops 3341.

---

## ops 3366–3367 — Supabase per-user layer: install verify + 3 live defects fixed (2026-07-17)

VERIFIED CORRECT (runner-side real requests): project bdmjenqcyvzouusfcgow alive (GoTrue v2.193.0); justhodl.ai/auth-config.js live + matches repo (enabled:true); **profiles table EXISTS with RLS ON** (anon filtered); email confirmations ON (prod-correct); /stripe-webhook signature enforced + worker secrets present (400 unsigned, not 503); /billing-portal 401 unauth; /create-checkout returns live checkout.stripe.com URL carrying metadata[plan]; anon device /userdata roundtrip intact.

FIXED (were live defects):
1. **`/userdata/self` returned 400 BEFORE auth** — uid "self" (4 chars) failed the length gate ahead of Bearer verify, so the drawer's signed-in favorites/theme sync (SYNC_URL=/userdata/self) was a **silent no-op** (client swallows errors). ⚠️ corrects 3292 memory: "tokenless=403 proven" was wrong — it was 400, and the authed path was equally dead. Now: `self` exempt from length gate, binds to verified uid, **unauthed self = hard 401** (no anon:self bucket possible). Re-gated ops 3367 PASS_ALL (tokenless 401, garbage-Bearer 401 on self AND ≥8-char uids, anon roundtrip intact, short-uid 400 kept).
2. **Webhook PATCH→UPSERT**: `PATCH profiles?id=eq.` matches 0 rows when the profile row doesn't exist → paid plan silently lost. Now POST `on_conflict=id` + `Prefer: resolution=merge-duplicates`; stripe_customer_id only set when present.
3. **NEW worker `GET /plan/self`** (Bearer → KV `plan:<uid>` → profiles via service role → "free") + additive auth.js `_loadTier` fallback = server-authoritative plan that survives any client/RLS misconfig.

Repo gains `supabase/setup.sql` — idempotent DDL (profiles + RLS select-own + signup trigger auto-creating rows + backfill + sanity readout).

PENDING KHALID (once, 30s): Supabase → SQL Editor → paste supabase/setup.sql → Run. Guarantees the **signup trigger** (only unproven leaf — confirm-ON blocked the JWT gate) + backfills existing users. Later, when plans are defined: create real Stripe products → add price IDs to `stripePrices` map in auth-config.js, swap pk_test→live, and register `https://justhodl-data-proxy.raafouis.workers.dev/stripe-webhook` in the Stripe dashboard. Entitlements map (free/pro/elite feature sets) already in auth.js awaiting his tier definitions.

Next ops 3368.

---

## ops 3368 — Drawer COLOR TAGS (2026-07-17, additive)

jh-nav-drawer.js: 5-color tags (red/orange/yellow/green/blue) on every row incl. favorites — unlimited pages per color, multiple colors per page. Row dots + 🎨 button → fixed-position swatch popover (toggle each color); filter chip strip under search (per-color counts, click filters, ✕ clears, stacks with search via guarded second input listener). Storage localStorage `jh_tags` {href:[colors]} (500-path/sanitized cap). Sync: pushSync blob += `tags`; pullSync per-page UNION merge (never-shrink, favs doctrine) → pushes back when local contributed. Filter/tag toggles route through full `render(lastM)` for clean visibility state. GEN 3335→3368. ⚠️GOTCHAS: CSS.escape inside quoted attr selectors breaks nwsapi/jsdom — plain href works (paths are safe); render-truth harness = jsdom@24 + `runScripts:"outside-only"` + `w.eval(src)` + fetch stub, remember `process.exit(0)` (jsdom keeps loop alive) and that dot-count asserts must scope to ONE `.jhtag-dots` span (favorites row duplicates pages). Harness PASS_ALL 9 behaviors pre-push; ops 3368 gates live GEN/markers + tags-survive-blob.

Next ops 3369.

---

## ops 3369 — Drawer CACHE-PICKUP root-cause (2026-07-17)

⚠️⚠️ WHY 3368's tags were invisible: committed HTML carried a FROZEN literal `jh-nav-drawer.js?v=3335` (373 pages, +1 stray v=3407) while the pages.yml stamper only replaced the UNVERSIONED src — and only on index.html (3272c). Stamper = silent no-op fleet-wide → every drawer JS change since the 3335 freeze never reached clients (clients+CF keyed on the old ?v URL). FIX: source normalized to unversioned src (374 pages; screener/ source untouched — protected — its deploy artifact still gets stamped), pages.yml step now regex-stamps ALL _site *.html with md5-8 of jh-nav-drawer.js and tolerates any pre-existing ?v= (freeze-proof). ⭐ GATE DOCTRINE: always gate the URL the PAGE references (extract src from live HTML, fetch that exact URL), never a synthetic cache-busted one — 3368's G1 passed while clients were stale.

Next ops 3370.

---

## ops 3370 — CONTENT-ADDRESSED SHARED ASSETS, fleet-wide (2026-07-17)

3369's defect was a CLASS: 39 local js/css (jh-page-ai 324 pages, interp-kit 133, jh-enhance 59, auth 17…) shipped unversioned → any change could strand in browser/CDN cache. NEW scripts/stamp_assets.py (single source of truth, used by pages.yml AND ops gates): TOPOLOGICAL versioner — deps version first, referrer bytes finalized (deps' ?v baked) BEFORE hashing referrer, so edits propagate transitively (auth-config→auth→drawer→all pages). Handles the drawer's DYNAMIC injection chain ("/auth-config.js"→"/auth.js"). ⚠️GOTCHAS learned: (1) naive fixpoint NEVER converges when a file both refs and is ref'd — hash churn forever; (2) SELF-references must be skipped in rewrite (KeyError mid-file + they're runtime string compares, e.g. auth.js doc/src checks — stamping would break them; refs set already strips self for topo); (3) graphlib ts.add(node,*deps) + belt-and-braces deps-first position assert. Proof battery pre-push: DETERMINISTIC / IDEMPOTENT (rerun=0 changes) / TRANSITIVE / dynamic refs baked / self-ref preserved / zero unversioned. pages.yml step replaced (3369's regex drawer stamp → stamp_assets.py). Gate doctrine: ops replicates expected versions via the SAME module on tmp copy of repo assets; gates = live stamps==expected, full-body md5==version (topo ⇒ version IS final-bytes hash), auth.js "/plan/self" finally client-delivered (3366 was cache-stranded), drawer bakes exact auth ?v.

Next ops 3371.

### 3370→3371 addendum — reskin-aware gating
⚠️⚠️ reskin_site.py transforms **.js/.css/.svg too** (not just HTML) in _site before stamping — deployed asset bytes ≠ repo bytes by design. 3370's G1/G2/G4 "fails" were the GATE's model, not production: live stamps were self-consistent content-addresses of reskinned bytes (proof: reskin-aware replica == live 7/7 exact). DOCTRINE: expected-hash replication must run the same pre-transforms as pages.yml (copy assets → reskin_site.py → stamp_assets.compute_versions). G3 confirmed auth.js "/plan/self" client-delivered. Next ops 3372.

---

## ops 3372 — HOT-MONEY drilldowns fixed + Asia/Europe STANDING FOCUS (2026-07-17)

hot-money.html "Inside the hot countries" was empty: (1) FMP /stable/etf/sector-weightings + /stable/etf/holdings had ZERO rename-resilience — non-list responses silently produced empty sectors/holdings (2026 rename class); (2) drill gated on top-6 INFLOW RANK, so HK/TW/KR/CN/Europe vanished whenever LatAm/MEA led. Engine v1.4.0: _fmp_list endpoint LADDER (etf/x → etf-x spellings, logs winning rung), _pctf %-tolerant weights, asset|symbol + name|securityName field fallbacks, shared _drill_one path, _day_changes with ONE comma-joined /stable/quote batch + per-symbol fallback, and FOCUS set drilled EVERY run (Asia: HK/TW/KR/CN/JP/SG/IN + Europe: DE/UK/FR/CH/NL/IT/ES/SE) marked focus:true, budget guards 800/820s (timeout 870). Feed adds focus_list/focus_note. Page ADDITIVE: 🎯 focus badge in drill header + legend line. Gates: deploy-settle+zip-marker (3342 race doctrine) → Event invoke → fresh v1.4.0 feed → core-4 Asia populated (≥3 sectors, ≥8 holdings) → ≥5 Europe + ≥5 Asia populated → ≥60% momentum coverage → live page 🎯.

Next ops 3373.

### 3372→3373 addendum — probe doctrine
⚠️ Two PROBE gotchas (product was fine): (1) NEVER append cache-busters to PRESIGNED URLs (Lambda Code.Location) — query mutation breaks the S3 signature → 403; fetch verbatim. (2) Emoji written as JS-escaped \ud83c\udfaf inside templates ships those ESCAPE BYTES in HTML — grep pages for bytes-as-authored, not rendered glyphs. hot-money live: 19 drilled (HK FinSvcs 42.5% · 1299.HK/0388.HK…), Europe 8/8, momentum 15/15; original-spelling FMP endpoints answered (rung 0) — emptiness was brittle parsing (% floats / asset-vs-symbol) + rank-gating, both now hardened. Next ops 3374.

---

## ops 3374 — FMP-ETF FLEET HARDENING (2026-07-17)

Class-sweep after 3372: 5 /stable/etf consumers audited. cef-discount CLEAN (etf-info + guards); hot-money fixed 3372 (keeps local copy). NEW ⭐ aws/shared/fmp_etf.py = fleet single-source (pctf %-tolerant, get_json, _ladder slash→dash spellings, holdings/sector_weightings raw, top_holdings convenience; stdlib, never raises, [] on fail). Patched: etf-constituents → shared (schema byte-compatible: stock now asset|symbol, weights pctf); industry-rotation → _wpct coercion (sort key TypeError'd on "%"-strings); theme-rotation-engine → its 2-rung ladder was the SAME URL TWICE (no-op!) → real dash rung + tolerant float (⚠️ NO config.json in repo — source-hardened only, deploy state recon'd not forced). Gates: settle+zip-bundle markers (shared *.py rides every zip; transitive-closure redeploys importers), EC Event invoke → constituents file 50/50 weights numeric desc sum-sane, IR static, TR recon informational.

Next ops 3375.

### 3374→3375 addendum — EC healthy; probe-origin doctrine
ops 3375 ground truth: etf-constituents (shared fmp_etf aboard) runs in **15.4s**, 284/289 ETFs written fresh (SPY NVDA 7.89/AAPL 7.37/MSFT 4.50, sum63.3; 2,258-stock exposure map; universe 300 incl SPY/QQQ/IVV). 3374-G2 "no fresh file" = WRONG ORIGIN: ⚠️ justhodl.ai only exposes data/* — S3-native prefixes (etf-flows/*, userdata etc.) 404 at the site host; gate them via boto3 list/get from the runner. BONUS: deploy-lambdas transitively redeployed theme-rotation-engine (live, timeout 900) from the shared-module change despite no repo config.json — TR's fixed dash-rung ladder is LIVE. Fleet sweep CLOSED: 5/5 /stable/etf consumers hardened or verified clean. Next ops 3376.

---

## ops 3376 — SETTINGS.HTML V3 + drawer ⚙ + TR config codified (2026-07-17)

⚠️ LIVE DEFECT on settings.html since it shipped: `<script src="/jh-nav-drawer.js" defer>` NEVER CLOSED → the following inline JH_SETTINGS_V2 block (theme buttons + billing-portal handler) parsed as that script's CONTENT = inert. Fixed. ADDITIVE V3: ⭐/🎨 synced favorites+tags card (Bearer /userdata/self + nav-manifest title join, read-only mirror; TAG_HEX local), checkout=success|cancel toast + history.replaceState (worker already redirects here), drawer signed-in row gains ⚙→/settings.html. Side-task: theme-rotation-engine config.json RECONSTRUCTED FROM LIVE (get_function_configuration + list_rule_names_by_target→describe_rule) and written by the ops for auto-commit — repo=truth restored. ⚠️GOTCHA class: unclosed <script src> silently swallows the next inline block — grep pages for `defer>` not followed by `</script>` when wiring dies mysteriously.

Next ops 3377.

---

## ops 3377 — JSI DECISION LAYER v1.9.0 (2026-07-17, ADDITIVE — Khalid: keep everything, add)

justhodl-stress-index 1.8.0→1.9.0: payload.v2 (try-wrapped, can never break core) computed from the in-hand 1990 spine + NASDAQCOM (fred_full): [1] fwd-return ATLAS by EXPANDING percentile decile (as-known-then, bisect.insort O(n log n)) + by regime, windows 21/63/126/252 (grid=business-daily), _stats med/p25/p75/pos%; [2] EPISODES ≥90th full-sample pctile spells (gap≤10 merge, ≥5d) w/ NASDAQ max-DD during + fwd63 from peak, current.active; [3] VELOCITY Δ5/Δ21 + 21d-change z, FLARE=vz≥2 & jsi≥40; [4] regime PERSISTENCE (run-length, per-regime median spell, pts-to-next-threshold); [5] DIVERGENCE COMPLACENCY(pct≥80 & NAS21>+3%)/STRESS_REPAIR + complacency-days own fwd-3m stats; [6] MOVERS from jsi-overlay-history Δ5-sessions (no spine-internals risk); [7] SIGNAL_STATE vs prev OUT_KEY read. PAGE: hidden #jsi-v2-sec (chips/atlas-highlighted-decile/episodes/movers-bars/divergence) via renderV2 wrapped after render. SENTINEL: jsi_regime/flare in snapshot()+diff() → Telegram on flip/flare-onset. Gates incl atlas sanity d9_3m<d2_3m + episodes span 2008&2020.

Next ops 3378.

### 3377→3378 addendum — atlas gate doctrine + FINDING
⚠️ Gate doctrine: never assert a DIRECTIONAL market hypothesis as a sanity check — assert invariants (coverage, quartile ordering, bucket validity) and record direction as a FINDING. ⭐ FINDING (1990→, NASDAQ, expanding-pctile honest): decile-9 stress → 3m median +8.2% vs decile-2 +3.1% (n=724) — peak JSI stress = contrarian buy historically; SVB episode (2023-03-09→17, peak 73.1) → +17.1% next 3m. Live now: JSI 35.97 NORMAL 73d (median dur exists), pctile 27.2, decile 2, no divergence, movers live 8. Next ops 3379.

---

## ops 3379 — SIGNALS GRADING LOOP RESURRECTION (2026-07-17)

⚠️⚠️ FLEET FINDING: ~40 direct schema-v2 emitters wrote rows the outcome-checker cannot score — TWO orthogonal breaks: (1) check_windows WITHOUT check_timestamps → checker's window loop iterates {} → pending forever; (2) measure_against = LITERAL string ("ticker","ticker_vs_benchmark","ticker_vs_acwx","ticker_vs_btc","basket_vs_benchmark","benchmark_forward_return") which the checker PRICES (line: ticker = measure_against or ticker). Equity picks only graded because justhodl-signal-harvester re-logs top_picks with the CORRECT contract (measure_against=SYMBOL, precomputed ts, "day_N" keys) — harvester = the Rosetta template. Macro emitters (nyfed-pd/ofr-stfm/settlement-fails/hot-money) were dead-on-arrival. FIX (single-point): outcome-checker in-loop NORMALIZER — synthesize check_timestamps from logged_at+window ("day_" stripped), recover symbol from signal_id "type#TICKER#date" regex [A-Z0-9.\-\^=]{1,10} — heals ALL past+future rows without touching 40 sources. ⭐ NEW aws/shared/signals_emit.py (harvester contract verbatim: log_signal + yprice Yahoo-keyless + dedupe ConditionExpression) for all NEW emissions. hot-money emits clean; stress-index v1.9.1 JSI closed loop: jsi-episode-entry(UP 21/63, atlas-backed)/jsi-flare(DOWN 5/21)/jsi-complacency(DOWN 21/63) on TRANSITIONS via shared, QQQ directional. ⚠️ outcome-checker had NO SCHEDULE anywhere (config cron None) — G0 recons live and CREATES Scheduler cron(0 8 ? * MON *) + codifies config. Gotcha: outcomes key = f"{signal_id}_{window}". Synthetic-legacy E2E gate proves both normalizations in one pass, then deletes itself.

Next ops 3380.

### 3379→3380 addendum — E2E proven + caveat
E2E PROVEN: synthetic row with BOTH defects (literal ma + day_-windows, no ts) → normalizer recovered QQQ from signal_id, synthesized timestamps, scored both windows → status=complete, return −1.41% vs 40d baseline, UP judged correct=False (honest). Stray cleaned. ⚠️ Invoke doctrine: account Lambda throttling is real — ALWAYS invoke_resilient (backoff on TooManyRequests, ≥5 tries); retries=1 sync invoke = fatal. Checker live cadence = 2×/day (21:29 + 23:00 rules; "-4h" name is a lie) → codified; backlog auto-resurrects within hours. ⚠️ CAVEAT: checker prices windows at CHECK-TIME (current price) — long-elapsed backlog windows all grade vs today's price (day_5==day_21 return on old rows); fine going forward (2×-daily), historical-close scoring = future checker-v3 item. JSI signal_state ARMED (NORMAL, no transitions). Next ops 3381.

---

## ops 3381 — JSI CHART V3 + FIELD COVERAGE (2026-07-17, page-only)

Coverage audit: page NEVER fetched data/jsi-history.json (daily 1990→) — now loaded in parallel (weekly fallback), and missing fields surfaced: history_span+spine_weight_mode+elapsed_s meta line, historical MIN beside max, v2.signal_state chip (armed/fired/↷). CHART V3 (Khalid-authorized graph rework, ids/identity kept): regime BAND rects CALM→CRISIS w/ right-edge labels; episode shading (.jsi-ep from v2 list, binary idxOf); crosshair group #xh + regime-aware tip; pulsing now-dot + right-edge value; RANGE chips ALL/10y/5y/1y; VIEW chips Level(default)/Δ12m pts/YoY % — ⭐ transforms computed on FULL series BEFORE range slice (12m lookback survives 1y view), OFF=252 daily/52 weekly; Δ-views: .jsi-zero line + red/green split fill via gradient stop at zero-fraction, bands+crisis marks OFF for clean read; downsample ≤1500 pts; st.lastN debug. Harness gotchas: strip external <script src> AND normalize dangling src-tags before jsdom; stub getBoundingClientRect for crosshair. 11-behavior jsdom PASS_ALL.

Next ops 3382.

---

## ops 3383 — JSI THREE ADDITIONS (2026-07-17): regime atlas table · spine sparklines · record readout

Engine v1.9.2: build_spine_series returns 6th element sparks — per-comp mapped stress sub-scores z_to_stress((xform-mu)/sd*pol), last 780 daily present pts stride-5 (~156 weekly) + today appended; emitted payload.spine_sparks {sid:{label,points[{d,v}]}}. ⚠️ early-return tuple must grow too ([], [], {}, [], mode, {}). PAGE: (1) #v2-atlas-regimes full table CALM→CRISIS reusing decile cell(), current regime ◀ now highlighted; (2) spineRow gains .jsi-sparkslot span; renderSparks() fills 90×22 min-max-normalized polyline, stroke=stressColor(latest), title=3y range — called after spine-list innerHTML; (3) #kpi-record-dist under max-date: pts-below-record · %-of-peak · highest-since (backward scan of d._daily), AT-RECORD state red. Harness 17 behaviors PASS_ALL.

Next ops 3384.

---

## ops 3384 — SOVEREIGN STRESS v2.0.0 (2026-07-17): CL/PE/NL + ledger + league + coverage

Engine: WGB module generalized — shared wgb_entry(name,slug); NEW wgb_sovereigns (7: KR/SG/HK/TW + chile/peru/netherlands) w/ WGB_REGION + COUNTRY_ETF (ECH/EPU/EWN/EWY/EWS/EWH/EWT/EWG/EWQ/EWI/EWP/GREK); asia_sovereigns UNCHANGED (page back-compat). HISTORY LEDGER data/sovereign-stress-history.json (rows[{date,europe,countries:{name:{s,cds,comp}}}] 400d, same-day replace) → deltas{d5,d21}. Transition signals via shared signals_emit: score≥65 & Δ5≥+10 → country-ETF DOWN [5,21] (sov-stress-spike). Payload +version/wgb_sovereigns/deltas/signals_fired/history_key, schema 2.0. PAGE (all legacy kept): 🌐 bond desk region-grouped (flags/rating/ETF/CDS+dflt-prob/CB rate, Δ5 chips, 📡 signal banner), 🏆 league table (bond-desk|SovCISS|composite unified + 60-session sparks from ledger), 📈 markets card (VIX/S&P/spreads + bond_market_read — WERE NEVER DISPLAYED), 🔗 cross-checks + src-meta (version·sources·errors·runtime), synth-read para into hero (JS-created). Coverage now total. ⚠️ heredoc python triple-quote replace() with ''' blocks = SyntaxError trap → write patch to /tmp/*.py file instead. Harness sov.js 13 behaviors.

Next ops 3385.

### 3385 addendum — Sovereign YoY chart
Engine v2.1.0: CISS+SovCISS loops enrich in-hand series with yoy_pct+level_12m (pct_change/val_days_ago 366); WGB names get yield_yoy_bp via FRED OECD IRLTLT01{NL,KR,CL}M156N + yoy_note; source-less 4 (PE/SG/HK/TW) carry "ledger accruing — unlocks automatically" note; payload yoy_chart sorted |move|. PAGE 📊 tornado (center-zero dual bars, 2 labeled groups: ECB true-12m vs 10Y-Δbp proxy) via renderYoY inside renderSovV2. Doctrine: never fake 12m history — enrich real series in-loop, proxy where honest, note where absent. Next ops 3386.

---

## ops 3386–3387 — GSSI: GLOBAL SOVEREIGN STRESS INDEX 1990→ (2026-07-17)

⭐⭐ NEW build_gssi() in justhodl-sovereign-stress (v2.2→2.3): 18 10Y spreads vs bloc safe asset (EU vs IRLTLT01DEM156N: IT ES PT GR FR NL FI IE BE SE; world vs USM156N: GB JP CH KR CA AU MX CL TR) + canary block 30% (CH unemployment 12m-Δpp ladder LRHUTTTTCHM156S→LMUNRRTTCHM156S, CHF DEXSZUS 63d inverted, JPY DEXJPUS 63d inverted, gold GOLDAMGBD228NLBM 63d) — full-own-history z → logistic 100/(1+e^-1.1z) → 0.70·spreadmean + 0.30·canarymean of PRESENT comps; identical across eras. Writes data/sovereign-gssi.json (weekly series w/ yoy via calendar-bisect, latest, components, crisis_scorecard). ⭐ VALIDATION (fixed rule pctile≥85 crossing OR Δ6m≥+12, window [start−270d,+60d], zero fitting): 8/14 detected — Lehman +198d EARLY (Mar-2008 warning), Euro-2011 +61d early, COVID −10d (exogenous, confirm-lag); misses = equity/terror events (specificity, not failure). Runtime 24s for 22 full FRED histories. ⭐ BRAIN SCAN (data/brain.json): japan 1487·china 828·gold 813 covered; TURKEY 159 + ARGENTINA 31 uncovered → added to desk (TUR/ARGT, region "EM Canaries") + Turkey 10Y into GSSI. Switzerland: desk (EWL, CDS 6.76bp stress 16.3) + IRLTLT01CHM156N proxy + CH-unemployment & CHF canaries. ⚠️ SG (MAS) + HK (HKMA) endpoint guesses 404'd round 1 → v2.3.0 wide ladders (MAS datastore_search underscore form + apimg-gw; HKMA govt-bond-benchmark-yield-daily + 3 alts). Page: GSSI line chart (YoY default + Level, crisis vlines + amber warning dots w/ lead tooltips, crosshair), scorecard table w/ lead days, canary strip; FLAG map extended. Gotcha: ops heredoc STILL bites on unclosed ''' — always close before trailing statements.

### 3388 addendum — GSSI MATH v2 + BAROMETER (Khalid: "math ok but not very good")
Self-critique of v1 (all chart-visible): full-sample level-z = regime contamination + future info; plain mean drowns the tail; no velocity, no co-movement. ⭐ v2 (engine 2.4.0): s_c = 0.45·L(z_lvl)+0.55·L(z_vel63) both EXPANDING-z (Welford incremental, min 756); SPREAD = stress-weighted intensity (w=1+s/100) × co-movement amplifier (0.75+0.5·ρ̄; ρ̄ via dispersion identity ρ=(N·Var₁₂₆(m)−1)/(N−1) on expanding-σ-standardized velocities — O(n·N) no pair loops); GSSI=0.72·SPREAD+0.28·CANARY. Feed: latest.breadth_pct+comove, weekly rows b/c. PAGE: semicircular BAROMETER svg (5 regime arcs, needle, pctile·YoY·breadth·comove) replacing text KPI. Baseline recorded math_baseline v1=8/14, Lehman +198d/2011 +61d/COVID −10d for comparison.

### 3388–3393 addendum — GSSI MATH v2 PROVEN (v2.4.4 PASS_ALL)
⭐⭐ v2 vs v1 leads (fixed rule, no fitting): Lehman +198d→+262d EARLY (late-2007/BNP era); Euro-2011 +61d→+224d (warned late-2010); DOT-COM missed→+164d (velocity term caught 1999 tightening); Tequila +233d, Greece-2010 +233d; COVID −10d unchanged (exogenous, correct); still 8/14 but stronger set. Live now: GSSI 36.1, 32.5pct, breadth 0%, comove 0.00 (calm+idiosyncratic), YoY −4.0%. DEBUG SAGA gotchas: (1) [skip-deploy] on a commit carrying engine fixes = fix never ships — check what a commit CONTAINS before tagging; (2) sed-clone source may already be in ran/ (pending→ran lifecycle) — && chain died silently, engine fix rode the next skip-deploy commit; (3) splice-indent wound: replaced-block tail (`vals=[...]`) left at loop depth → IndexError iter-1; (4) ⭐ LOCAL-REPRO HARNESS doctrine: exec engine source into a ModuleType with stubbed fred()/s3 → full build_gssi runs in 0.1s locally, catches runtime bugs pre-push (this caught #3 instantly after two blind CI round-trips); (5) FRED dead ids surfaced by 400s: IRLTLT01TRM156N nonexistent (TR desk-only now), LBMA GOLD* discontinued 2022 → krw_canary DEXKOUS (pol +1, the 1997 textbook tell); LRHUTTTTCHM156S 400 but ladder rung-2 LMUNRRTTCHM156S carries CH unemployment. Barometer gauge live (5 arcs, needle, breadth·comove stats).

### 3394–3395 addendum — sovereign arc CLOSED
Strip (breadth bars + comove band under GSSI chart) + sentinel sovereign/GSSI watch (europe regime flip, sovereign crossing ≥65, GSSI band change, comove≥0.5 systemic alert) LIVE. ⚠️ SG/HK hunt DEFINITIVE after 3 empirical rounds: HKMA apidocs 404 (no harvestable public paths), MAS CKAN empty/404, data.gov.sg catalog has NO SGS yields (fuzzy junk). Closure: v2.4.6 DORMANT ledger-YoY path — when sovereign-stress-history rows≥300 (~2027-07), wgb names lacking yield_yoy_bp auto-gain stress_yoy_pct ("true stress YoY — own daily ledger"); tornado renders it when it appears. Never guess external endpoints twice — harvest catalogs or use own ledger.

---

## ops 3396 — SECTOR-ETF TECHNICAL LAYER (fleet-audit gap fill, 2026-07-17)

Khalid audit ask (sector ETFs: acc/dist, institutions, options, $ in/out, double tops/bottoms, MA 200/100/50/20). MATRIX: acc/dist=accumulation-radar (Wyckoff, all 11 XL*) ✓ · institutions=capital-flow v2+13F ✓ · options=options-flow XL* flow_5d ✓ · $ flows=sectors-tornado/etf-fund-flows/fusion ✓ · GAPS: chart-patterns had detect_double_top/bottom but S&P500-STOCKS-only (0 XL*); NO engine computed sector-ETF OWN 20/50/100/200 ladder — 100-DMA existed NOWHERE fleet-wide (FinViz lacks SMA100). FILL (extend-don't-duplicate): chart-patterns sector_scan() 11 SPDRs+SPY (same detectors + ladder w/ fresh_cross 5-session + posture FULL UPTREND/DOWNTREND/MIXED) → payload.sector_etfs; sector-capital-fusion technicals join (defensive _walk over acc-radar for phase map, options-flow for flow_5d; ETF_OF GICS→SPDR map; display-layer only — six verdict lenses untouched) → row.technicals + technicals_joined; sector-flow.html self-contained appended 🩻 section (chips 20/50/100/200 ↑/↓ ⚡, pattern badge, Wyckoff, options). Gotchas: replace-gymnastics corrupted P2 in-memory — ast.parse BEFORE write saved disk; anchor was `doc = {` not `out/payload = {`.

Next ops 3397.

### 3397–3400 addendum — technicals join hardened
Wyckoff join saga: walker symbol→ticker fix (3397, 0→5) → canonical acc-radar etf_phases map (3398, doctrine: consumers read the MAP not curated [:12] buckets) → deterministic chain radar-feed-fresh→fusion (3399, exposed etf_phases=0) → root: radar rows key "ticker" NOT "symbol" — comprehension filter dropped all (3400 fix symbol|ticker). FINAL: etf_phases=35, XL*=11, sectors_with_wyckoff=11/11 PASS_ALL. ⚠️ Pattern sensitivity FINDING: detect_double_top fired on 10/11 XL* — stock-tuned geometry over-triggers on smooth ETF series; v1 shipped honest, quality-threshold pass = known follow-up. Live ladder 2026-07-17: FULL UPTREND XLE/XLF/XLP/XLRE/XLV · FULL DOWNTREND XLY (lost d20/d50/d100 fresh, below 200d w/ XLC) · XLP lone double_bottom + reclaimed d20/d50/d100.

---

## ops 3402–3411 — THE MONETIZATION BUILD-OUT (audit → all missing items shipped, 2026-07-17)

Khalid: "build all that's missing but not #8 (kill-criteria)". Delivered:
⭐⭐ **#1 justhodl-proven-portfolio v1.1 (THE COMPOSER)**: scorecard-gated (explicit proven/alpha, else n≥20 hit≥55% IR>0 = PROVISIONAL, self-upgrading) → 10d live signals from justhodl-signals → strongest-per-ticker → quarter-Kelly via data/sizing.json (fallback confidence-scaled) → **top-40 by conviction, 8% cap, 100% gross** → daily NAV ledger vs SPY (data/proven-portfolio-history.json) + attribution by signal family (#10b). **DISCOVERY: 70 families already PROVEN** → launched mode=PROVEN day one; book MSFT/GILD/FOX/WTFC/META/AMZN; NAV 100.0 inception 2026-07-17; Scheduler 21:45; page proven-portfolio.html.
⭐ **#2**: best-setups SELF-GRADES — top-15 → type "best-setup-stack" [5,21,63] via signals_emit (16 rows proven in DDB). **#7**: rel_volume (finviz walker) + entry_confirmed(≥1.3) + hold_horizon_days (halflife median 21) on every setup. **#6**: suggested_structure v1 (options-confluence score≥70→call debit spread; entry_confirmed→3-6m calls half-size; else shares+covered calls) — IVR feed absent, noted.
⭐ **#5 justhodl-short-book v1.0**: bear lenses (dark_distribution 25 · forensic SBC_WASH/MGMT_SELLING/DEATH_SPIRAL 20ea cap40 · beneish>-1.78 20 · 200-DMA cross-down 20 · wyckoff DIST/LIKELY_TOP 15 · double_top 10), ≥2 lenses, top-20, **pair-vs-sector-ETF benchmark grading (DOWN)**. Live: WDC 45 · BLK/JEF 40 (dark+wyckoff) · SNDK · SNPS; 6 graded day one; Scheduler 21:50; short-book.html.
⭐ **#9**: weekly-ai-review ALPHA COUNCIL via anchor-free wrapper (rename core + wrapper appends _alpha_council_block: NAV+top5, stack w/ ✓entry, short book w/ pairs, index-recon forced-flow dates) — Sundays.
⭐ **#10a CHECKER-V3**: get_price_at(ticker,date) Yahoo 2y; windows >2d old priced AT THEIR DATE (cache keyed (tk, as_of|LIVE), both ticker+benchmark legs). **E2E PROOF**: synthetic SPY backdated 40d → day_5 741.75 +0.57% vs day_21 728.99 −1.16% (impossible under check-time pricing). #3 struck: justhodl-engine-trust ALREADY the regime plug (base×regime Wilson-LB, daily 12:30, consumed in ranker).
GOTCHAS: [1] invoke-after-deploy MUST wait LastUpdateStatus==Successful — zip markers lie mid-update (two stale-code runs). [2] ddb Scan UNPAGINATED = phantom zeros (short-book "0 logged" was 6). [3] CW filter_log_events filterPattern with hyphenated/quoted phrases = false negatives ×2 — **gate on DATA, never on log greps**. [4] payload-dict anchors: insert before the `X = {` opener (regex last-opener-before-anchor), never inside. [5] local-repro (stubbed s3/ddb) cleared composer+short-book pre-push in <1s each.
Next ops 3412.

### 3412–3414 addendum — CROSS-ENGINE JOINS WAVE 1
Ten-join audit: GAPS 1,2,3,4,6,7,8 · BUILT #5 (best-setups ops-3145 already fuses earnings cal + squeeze + insider red-flag!) · PARTIAL 9,10. ⭐ SHIPPED: [#1] signals_emit _regime_snapshot() — every log_signal auto-stamps metadata.regime {jsi_decile,gssi_band,liquidity} (5-min cache, never raises); fleet-wide via shared redeploy; runtime rows stamp at first FRESH emission (same-day dedupe blocks re-proof — structural gate 3414). [#3+#4+#5-tag+#9-slice] composer v1.2: orthogonality cluster caps (25% gross/cluster), JSI≥75p→×0.85/≥90p→×0.70 + GSSI≥60→×0.85 gross throttle, earnings_in_window (LIVE: 27/40 names!), conflict-vs-short-book weight-halving. GOTCHAS: [1] '"mode": mode,' ambiguous anchor → keys landed in LEDGER row; unique-suffix anchors only. [2] `a or b or cl if isinstance(...) else []` ternary swallows or-chain → clusters always []; parenthesize. [3] jsi feed latest.pctile path returned default 50 — real key TBD (throttle neutral until fixed). Local-repro asserts (throttle/cap/conflict/earnings) caught both bugs pre-push. WAVE-2 QUEUE: #2 squeeze→short-book, #6 GEX walls→setups, #7 sector-fusion chip→setups, #8 playbook analogs, #10 feed-registry+SLA sentinel, jsi-pctile key fix. Next ops 3415.

### 3415–3420 addendum — JOINS WAVE 2 (PASS_ALL)
⭐ SHIPPED: [#2] short-book v1.1 squeeze guard (squeeze-fuel ≥70 exclude + squeeze_excluded list, 40-70 tag+conf×0.7). [#7] setups sector_context 24/25 via flow-state (rows key "name" NOT "sector") + universe.json ticker→sector map (setup rows had sector:null!) + canon aliases. [#8] playbook_context top-3 mined rules live (TV-note "ECONOMY CRASH LAGS YIELD…"). [#10] justhodl-feed-registry v1.2 (785 top-level feeds, 105 stale ledger, k.count("/")>1 skip, daily 07:30) + sentinel newly-stale Telegram. [jsi] composer v1.2.3 SELF-COMPUTED pctile from data/jsi-history.json {"series":[{d,v}]} → 29.7pct src=jsi-history.self (stress-index feed carries NO pctile). [#6] walls: dealer-gex underlyings lack per-ticker wall vocab — levels need computing from by_strike = honest future build; tolerant walker stays. ⚠️⚠️ TERNARY-PRECEDENCE BUG **3rd strike**: `a or b or c if cond else d` == `a or b or (c if cond else d)` — ALWAYS parenthesize feed-shape fallbacks. Gate-on-DATA doctrine held (CW filterPattern banned). Next ops 3421.

### 3422–3426 addendum — QUALITY CENSUS + ALPHA-TRIAGE (#1) COMPLETE (PASS_ALL)
⭐ CENSUS (3422): 7 pages w/ Math.random mock (ai_predictions/brain/chart-pro/dep-graph/journal/ofr/treasury-auctions); 3 on dead execute-api (ny-fed/ofr/treasury-auctions); 12 unshelled pages; 150 chart pages off jh-enhance; stale decision feeds regime-decisive-call 37d/backtest-summary 36d/finviz-signals-state 31d/market-internals-history 37d/engine-manifest 27d; political desk dead (quiver 22-47d); self-monitoring dead (eventbridge-audit 54d/system-audit 52d/_freshness-status 56d). 10-item list delivered; Khalid: start 1.
⭐⭐ ALPHA-TRIAGE (3423–26): dual-store study (902 on-row + 1,099 justhodl-outcomes rows → 699 synth; harvester families grade day_7/14/30 NOT 5/21/63; tolerant excess extraction). Codified rules: INVERT n≥60 + flip_hit≥0.65 BOTH halves + flip d21-equiv ≥+30bps after 10bps cost + ≥2 windows agree; hit≤5% big|excess|=FIX; else RETIRE. **VERDICTS: INVERT eng:ai-infra-stack (d14 n48 hit10.4% avg−18.5% flip 0.894/1.0) + eng:finnhub-signals (n66 hit10.6% flip 0.902/0.8). RETIRE ×8: ignition/accumulation-radar/radar-backtest/smart-money-13f/boom-radar/nobrainer_XOP/nobrainer_AIQ/attention-signals (h2 0.333 = regime-flip caught).** ACTIONS: data/signal-suppress.json (8 fams) consumed by signals_emit _suppress_set() 10-min cache → fleet-wide silence, PROVEN live (boom-radar emit logged=False); justhodl-inverse-harvester daily 22:10 mirrors INVERT fams as inv:<fam> FLIPPED dir same benchmark → graded FORWARD out-of-sample, PROVEN gate decides composer entry (E2E: synthetic finnhub UP → inv: DOWN mirror). NOTE: accumulation-radar suppression = EMITTED signals only; page/phases/fusion joins untouched. INVERT sources deliberately NOT suppressed (raw material). data/alpha-triage.json = verdict feed. Next ops 3427; census item #2 (dead decision-feed resurrection) queued.

### 3427–3437 addendum — CENSUS #2: DEAD DECISION-FEEDS RESURRECTED (PASS_ALL)
⭐⭐ AIPANEL CLASS (33 decisive-call panels + 53-context registry config/ai-brief-contexts.json): root cause TWO-PART — [a] justhodl-ai-brief-router had NO schedule (lost ~June 10; only externally-invoked alerts-digest ran) [b] /justhodl/llm/mode=on_demand cost-gate → anthropic_shim empty → ERR_CLAUDE ×47. FIX (respects cost doctrine): Scheduler justhodl-ai-brief-router-6h cron(20 1,7,13,19) + DETERMINISTIC FALLBACK — generate_one_brief wrapper (rename-core pattern): on ANY ERR_* status, _det_brief() composes {regime,confidence,one_liner,thesis,supporting_evidence,mode:"deterministic"} from context's primary_feed + cross_regime_fields, writes data/<output_key>.json. Iterations: ERR_CLAUDE-only→widen ERR_* (regime was ERR_EXC); ⚠️⚠️ PARALLEL-DEPLOY RACE: run-ops rotation executed OLD zip because settle-marker (_det_brief) existed in v1 too — ALWAYS bump a DISTINCT marker (DET_FALLBACK_V2) per behavioral change; primary-state extraction needed bounded BFS depth-3 over regime-ish keys (key-list guessing failed twice). LIVE 33/33: regime-decisive-call = "EASY · yield_curve=BULL_STEEPENER, vix=NORMAL_CONTANGO, dollar=LEAN PUMP". LLM layer auto-resumes when credits return (fallback only fires on ERR).
⭐ ENGINE-MANIFEST SELF-SUSTAINING: scripts/gen_engine_manifest.py (repo-grep: engine→written data/ keys + config description) + run-ops.yml step (after AWS creds, before Run script(s)) uploads on EVERY ops run → 694 engines/339 keyed, age≈0; ask-desk+strategist fleet map fresh forever.
⭐ ORPHANS RETIRED: config/feed-retired.json {backtest-summary, market-internals-history, finviz-signals-state} (writerless, zero consumers) honored by feed-registry v1.3 (retired flag, excluded from stale). REMAINING SMALL: spx-history-deep refresher (8 lambda consumers, 37d, low-harm static history) — future build. Census items 3-10 queued. Next ops 3438.

### 3438 addendum — CENSUS #3: MOCK-DATA PURGE (PASS_ALL)
Census overcount resolved: chart-pro/brain/journal/dep-graph Math.random = IDs/layout ONLY (clean). REAL offenders fixed via append-override pattern (OPS3438 script before </body>, dead bases blanked): ofr.html (6nl5fzfus7 gateway blanked, mock generator call removed, 9 legacy tabs → retired banners, lands on real 🚰 STFM tab) · ny-fed.html (jc6ripzwk1 blanked; live FR2004 banner from data/nyfed-primary-dealer.json — corp net/regime/financing; 7 tabs retired → primary-dealers.html) · treasury-auctions.html (klehdyiwrl blanked; real TreasuryDirect upcoming table from data/treasury-auctions.json {date,term,type,offering,reopening,cusip}; synthetic-variation block unreachable) · ai_predictions.html (mock confidence series neutralized; chartsSection hidden; live thesis table from data/wl-predictions.json {predictions:[{name,status,z}],horizon_weeks,n_theses}). Gates over LIVE URLs: dead-api absent + OPS3438+real-feed strings present + 3/3 feeds reachable. Next: census #4.

### 3439–3441 addendum — CENSUS #4 + #5 (PASS_ALL)
⭐ #4 POLITICAL DESK: root cause Quiver API **HTTP 401** (key/subscription expired ~June); justhodl-lobbying-intel ran daily, silently republishing 36d S3 cache (20k records) as fresh. HONESTY LAYER: both engines (lobbying-intel + political-stocks) stamp source_status/stale_cache/cache_age_h into feeds ("STALE_CACHE — Quiver API 401; data age 874.9h" live); political.html OPS3440 sticky banner reads flag. KHALID ACTION: renew Quiver key → SSM/env → desk self-heals next 16:00 run (fallback only fires on failure). political-stocks pattern: _stamp_quiver_cache_age() head-checks S3_QUIVER_CACHE_KEY age>48h at handler start.
⭐ #5 SELF-MONITORING: old trio (eventbridge-audit/system-audit/_freshness-status) = corpse feeds of DELETED engines — monitoring evolved into justhodl-fleet-error-monitor (5m) + fleet-freshness-monitor (30m) + fleet-monitor (3h) + schedule-liveness (daily 13:30). Corpses → config/feed-retired.json (list now 6). HEARTBEATS PROVEN: 0.02h/0.02h/1.52h/13.49h vs cadence maxima. No gap: schedule-coverage auditing lives in justhodl-schedule-liveness.
CENSUS SCOREBOARD: items 1-5 CLOSED (ops 3423-3441). Queued: 6 jh-enhance top-20 · 7 shell 12 pages · 8 flagship-tier proven-portfolio/short-book pages · 9 composer input hygiene · 10 per-feed SLA map. Next ops 3442.

### 3442–3445 addendum — CREATIVE ARC OPENER: REGIME BRIDGE + STEALTH FAMILY + SELF-UPDATING FADE DESK (PASS_ALL)
⭐⭐ [#7 BRIDGE] scorecard.by_regime + engine-trust regime-conditioning were STARVING: scorecard keys on flat outcome field regime_at_log; nothing wrote it. FIXED: signals_emit _regime_snapshot() += canonical "label" from data/regime-map.json regime.label (engine-trust's exact vocabulary — current_regime() reads regime-map→khalid-index→regime-read→macro-nowcast); outcome-checker REGIME_BRIDGE_V1b copies label (fallback J{decile}|{gssi}) onto EVERY graded outcome — on-row outcomes map AND justhodl-outcomes table row top-level. E2E both stores proven (TESTREGIME3445). ⚠️⚠️ NEW GOTCHA: table dict literal ALREADY had "regime_at_log": signal.get(...) further down — Python duplicate-key LAST-WINS silently nulled my value; grep dict literals for the key BEFORE inserting. by_regime matrices now fill as outcomes grade (7-30d maturity); engine-trust regime_factor sharpens automatically.
⭐ [#2] justhodl-stealth-flow v1.0 daily 21:35: fresh (≤2d) STEALTH-quadrant detections from data/etf-flows/event-study.json → graded UP [5,21] vs ladder bench, type "stealth-flow" (the 75%/+298bps cohort); PROVEN gate controls promotion. 0 detections day-one = correct sparsity (|z|≥2 + cooldown). Feed data/stealth-flow.json.
⭐ [#1] inverse-harvester v1.1 SELF-UPDATING: invert_families() reads data/alpha-triage.json verdict==INVERT at runtime (fallback 2 hardcoded); _families_probe event proves resolution. Future triage runs change the fade book with zero engine edits.
CREATIVE AUDIT VERDICTS (10 ideas): built-different #8 (rebalance-radar = quarter-end study); PARTIAL 1/7/10; NOT BUILT 2/3/4/5/6/9. Remaining queue: #6 auction-tail family → #9 cannibals+insiders → #4 credit composite → #3 free congress ingest → #5 equity FTD → #10 Proven Alpha Report. Next ops 3446.

### 3446–3451 addendum — CREATIVE #6 + #9 (PASS_ALL)
⭐ [#6] justhodl-auction-tail v1.0.1 (16:20 MON-FRI, after grader 16:00): fresh (≤2d) COUPON auction grades from data/auction-grades.json {graded_auctions:[{auction_date, security_term, security_type, overall_grade, dimensions.tail_bp.value,...}]} → two-sided family "auction-tail": tail≥+1.2bp or D/F → DOWN etf; tail≤−1.0bp or A → UP; tenor→ETF 20/30Y→TLT, 5/7/10Y→IEF, 2/3Y→SHY; benchmark BIL (pure duration); windows [3,10,21]. Bills EXCLUDED. Live extraction proven seen=1 coupon (fresh=0 Friday-correct). GOTCHA: live rows use overall_grade NOT grade; bills lack tail dim — tolerant "tail"-substring dim scan.
⭐ [#9] justhodl-cannibals v1.0.1 (21:55): data/share-flows.json shape = {"tickers": {SYM: row}} KEYED-BY-SYMBOL (no inner ticker — same gotcha class as dealer-gex/underlyings; direct .items() iteration, universe 649 proven). Screen: sh_3y_cagr_pct ≤ −2%/yr ∩ INSIDER_CONVICTION ∩ no SBC_WASH/MGMT_SELLING/DEATH_SPIRAL → top-15 → "cannibal-conviction" UP [21,63] vs sector ETF pair; rows carry buyback_net_yield_pct. Day-one screen=0 = correct tightness. Local-repro asserted both engines pre-push. CREATIVE remaining: #4 credit-composite → #3 free congress → #5 equity FTD → #10 Proven Alpha Report. Next ops 3452.
