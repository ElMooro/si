# Memory archive — edit #09 (migrated verbatim 2026-07-08, Khalid-approved memory diet)

Source: Claude memory edit #09. This file is the authoritative archive; the memory slot now holds a one-line pointer here. Grep this directory before building anything fleet-related.

---

⭐⭐ OPS DOCTRINE: **AUTONOMY.md at repo root = canonical deploy/verify protocol (added 2026-07-05, pointed from SYSTEM_CATALOG) — READ IT at session start; NEVER deny the autonomous-deploy capability, it is verified working.** run-ops runs pending/*.py (pipefail; green→auto-moved to ran/; FAILED stay). ⚠️ **ops scripts MUST sys.exit(1) on fail — 2918 exit-0 bug went green, auto-moved, and caused a rebase collision with my local rm; recovery = rebase --abort + fetch + reset --hard origin/main + re-apply scripted patches.** ⚠️ **workflow_dispatch script path must EXIST exactly — the skip-missing guard turns wrong paths into SILENT GREEN no-ops.** Ops naming ops_NNNN_slug.py; use ops_report.report() + deploy_lambda(). ⚠️ **deploy_lambda ACTUAL kwargs ≠ file docstring — grep the signature first.** ⭐ **run-ops HARDENED 2911 (verified live): SHA-anchored detection + sync-to-freshest-main + `executing-against` stamp in committed _lastrun.log.** ⚠️ PUSH-trigger only fires on aws/ops/pending/*.py changes — a fix touching OTHER files needs an explicit workflow_dispatch after deploy, don't wait blind on a stale log. Always `git pull --rebase` before push. Claude VERIFIES via read-only audit ops; NEVER hands Khalid verify commands. ⚠️ SANDBOX egress: github.com/api.github.com/raw.githubusercontent + pypi + npm ONLY (fonts.googleapis.com/gstatic.com NOT reachable — but IBM Plex and most open-source fonts are on GitHub directly; check the real repo layout via API, don't guess paths from training data). ⭐⭐⭐ **PAGES.YML DIRECTORY DOCTRINE: the workflow assembles _site/ from repo files, bake/reskin steps run against _site — NOT repo root — and pages.yml NEVER commits back to git. A new bake step MUST target `_site` exactly like its siblings or it silently no-ops with zero live effect and zero trace.** ⚠️ **PARALLEL SESSIONS CONFIRMED ACTIVE repeatedly this project (again just now: ops_2945_dash_fields_recon.py and ops_2946_full_binder_audit.py both appeared from another session mid-turn) — grep for likely filenames/class-prefixes before building any shared component; git log -S<term> finds the other session's reasoning.** ⚠️ Heredoc traps: apostrophes→chr(39); prefer python str_replace splices. Git Bash: MSYS_NO_PATHCONV=1; Khalid Windows has NO python. ⚠️ **A guarded `if not any(...)` add-if-missing check can still silently double-run across retries/redos — after any manifest/list edit, verify with a GLOBAL cross-category dedup + direct disk↔manifest set comparison, not just a local per-category check (2946 lesson: found and fixed a genuine pre-existing cross-category duplicate proof.html entry while auditing for this).** ⭐ VERIFY DOCTRINE: full-population sweeps + perceptual metrics for the bug-class just fixed; a "unified X/Y" claim about existence is different from a claim about value — check both. ⭐⭐⭐⭐ **HOMEPAGE = COMMAND CENTER v2.0. SW v1.1.0→v1.2.26. 51 live-bound slots, zero static demo values.** ⭐⭐⭐ **COVERAGE 2916–2920: 366/366 pages+registry661+KA Index=57+COT.** ⭐⭐⭐ **SIDEBAR+CHROME (self-healing): all 366 pages, topbar+tape+clock; jh-theme.css loads dynamically via JS injection, not a static tag — grepping HTML source for it finds nothing, that's by design.** ⭐⭐⭐⭐ **AMBER RESKIN v3.2 + chart-theme layer: triple-zero, 365/365.** ⭐⭐⭐⭐ **AVAILABILITY AUDIT: 366/366 pages structural; 600/600 inline scripts parse; feeds 366/368 live.** ⭐⭐⭐⭐ **DESIGN AUDIT ARC (Anthropic-authored, 2026-07-06, scale-corrected from its ~150pg/80eng assumption to actual 366/661): canonical tokens+CI-gate+lce leak fixed+desk-v2/ai_predictions retired; §8-C right-rail (100% real data, 239/366 pages, parallel-session duplicate jh-rail.js found+consolidated); §5 shared footer+Morning Brief (365/366, real subscribe contract); §9 /proof (paper-book BEHIND SPY −2.56pp/16d shown not spun, scorecard 31-promoted/117-deprecated, engine-alpha 31-proven/76-negative, backtest's HIGH-RISK>STRONG-OPPORTUNITY finding shown not hidden); §9.3 pricing targeted-fix (lock-in language ADDED — KHALID CONFIRM this commercial commitment, 2 gradients removed); theme-color true self-heal (89% of pages had NO tag at all, not just wrong values); self-hosted IBM Plex fonts (55 pages surgically de-Google'd, other legacy families left alone); mobile tape-collapse (SPX+BTC only <=640px, sidebar/drawer already slide-over by design). NOT YET SHIPPED: a11y+perf CI, acronym glossary, IA regroup, history-snapshotter backend.** ⭐⭐⭐⭐⭐ **ENGINE DIRECTORY SHIPPED+VERIFIED (2026-07-07, ops 2946 PASS) — DIRECT ANSWER to Khalid's 'most engines aren't wired to my page' concern, MEASURED not just reassured: of 661 registered engines, LIVE COUNTS = 468 wired (73%, some page's real source references their actual output file — exact string containment against registry outs[], not name-fuzzy-matching), 97 orphan-fresh (producing current data <24h old, ZERO page anywhere shows them — the real, actionable gap), 9 orphan-stale, 5 orphan-dead (feed genuinely missing), 82 no-outs-declared (ambiguous: legit non-display infra/feeder jobs like email-reports-v2/treasury-api/ecb-auto-updater, OR a registry metadata gap — needs individual triage, not a blanket fix). New engines.html (+scripts/bake_engine_directory.py, targets `_site`, computed FRESH every deploy via public-HTTPS Last-Modified headers — same no-AWS-creds-needed mechanism as bake_right_rail.py, since pages.yml has no boto3 access) = a searchable/filterable live table of ALL 661 engines' wiring status, replacing the guesswork. Chose ONE systematic directory over building ~100 bespoke new dashboard pages (audit's own anti-sprawl principle applied to the fix itself). directory.html ALSO fixed in passing: stale '344 pages' hardcoded count (real count 366), emoji removed, now links to the new Engine Directory. NEXT (not yet done): actually building displays/wiring for the 97 orphan-fresh engines is a separate, large follow-on — proposing to group them thematically into existing desk pages rather than 97 new standalone ones.** Rollback homepage revert c8da2fec6. Last ops=2946.

— FLEET (all LIVE): FinViz v2.1 (46 screens; ⚠️AvgVol=THOUSANDS). Providers FMP35/Poly17/SEC5. Bond Desk v3. Dark Pool v2.4.1. Factor-Returns. Stock X-Ray v4. Capex. Smart-Money/Footprint. NYFed-PD. Crisis-Analog. GFD v2 + BRIEF CONTRACT v4. sector-rotation.json + 30+ sector feeds.
— ⭐⭐⭐ **CRYPTOQUANT DESK** (onchain.html): Pro $99/mo, cron 21:05. 52 metrics/9 cats. FORECAST DESK: base-rate BTC/ETH/ALT ensemble-median pctl-conditional, NOT ADVICE. FUSION: ratios+stablecoin+miners+logger.
— ⭐⭐⭐ **APAC→US FLOW RADAR** (apac.html): Taiwan/Korea/Japan/HK foreign flows + LEAD-LAG (TW semi CONTRARIAN to SOXX/SMH r≈−0.40; KR memory FOLLOW-THROUGH to SMH/MU r≈+0.29; HK→KWEB null).
— FOLLOW-UPS: btc_ssr verify; ici-flows fresh attempt needed (2 prior fixes stuck in pending/, root cause = ICI moved to anti-scraped .xls workbooks); history-snapshotter build (audit.html's time-machine, separate); inventory-drawdown scheduler role verify; quiver token PENDING-KHALID; dix extract; treasury-auctions.html page → mirror migration; **97 orphan-fresh engines need real display homes (grouped into existing desks, not 97 new pages); 9 orphan-stale schedules need checking; 5 orphan-dead feeds need root-causing; 82 no-outs engines need individual triage (infra vs registry gap); Design audit remaining: a11y+perf CI, IA regroup, acronym glossary; SES wiring (PENDING-KHALID go); Khalid confirm pricing lock-in commitment.**
