# CLAUDE.md

This repo is driven autonomously by Claude. Session protocol, in order:

1. Read **AUTONOMY.md** (canonical deploy/verify protocol + traps) and the
   protected "MASTER BOOTSTRAP" memory card.
2. `cat STATE.md` -- live ops-pipeline snapshot (next free ops number,
   pending queue, last verdicts). Auto-regenerated by run-ops after every
   run; trust it over manual recon.
3. Before pushing any ops script or lambda change:
   `python aws/ops/_preflight.py <changed files>` -- catches the failure
   classes that cost a full push/poll round trip.
4. Commit IS the deploy (pages.yml / deploy-lambdas.yml / deploy-workers.yml
   / run-ops.yml). Verify from the committed reports, never by asking Khalid.

Khalid runs nothing locally. Real data only. Audit before building.
